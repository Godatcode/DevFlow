{
  "name": "@devflow/shared-database",
  "version": "1.0.0",
  "description": "Database optimization and connection management for DevFlow.ai",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest --run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "pg": "^8.11.3",
    "pg-pool": "^3.6.1",
    "@influxdata/influxdb-client": "^1.33.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.10.7",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}